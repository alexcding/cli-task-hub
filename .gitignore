node_modules/
dist/

# Runtime data — user state, regenerated at runtime (never source)
taskhub.json
taskhub-snapshot.json
taskhub-jira-snapshot.json
taskhub.db
config.db
config.db-journal
config.db-wal
config.db-shm
data.db
data.db-journal
data.db-wal
data.db-shm
logs.db
logs.db-journal
logs.db-wal
logs.db-shm

# Logs (written to <userData>/logs in the packaged app; to repo root in `node server.js` dev)
logs/

# Intermediate icon build dir (the icon PNGs/ICNS themselves are committed)
build/icon.iconset/

# Env / OS
.env
.env.local
.DS_Store
*.log
